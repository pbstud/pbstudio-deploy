{% for entity in individual_packages %}
    {{ include('package/_package.html.twig', { 'package': entity, 'isGiftMode': isGiftMode|default(false) }) }}
{% endfor %}
{% if not isGiftMode|default(false) %}
{{ include('package/_gift_card_entry.html.twig') }}
{% endif %}
