fix(session): alinear conteos dashboard/listado y endurecer validacion de instructor

- corrige discrepancia de Bloque 5 entre KPI y listado de clases
- evita que el endpoint JSON de sesiones fuerce date_end=date_start
- agrega filtro de disciplina visible en listado para conservar filtros al buscar
- cambia join de perfil de instructor a LEFT JOIN en listado para no omitir sesiones
  cuando el staff no tiene StaffProfile (causa raiz del conteo 22 vs 18)
- exige instructor obligatorio a nivel de entidad (NotBlank)
- marca instructor como required en SessionType para creacion/edicion manual
- evita creacion de sesiones sin instructor en SessionDayController (guard clause)
- corrige validacion AJAX en edicion para enviar session[id] y excluir la misma
  clase al verificar conflicto de horario

Impacto funcional:
- el conteo del dashboard y el listado de clases quedan consistentes
- editar/guardar la misma clase ya no dispara falso conflicto de horario
- se bloquea persistencia de clases sin instructor en flujos manuales y masivos
